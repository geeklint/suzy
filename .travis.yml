dist: bionic
language: rust
before_install:
  - sudo apt-get -y install libosmesa-dev
script:
  - cargo build --verbose --workspace
  - cargo test --verbose --manifest-path suzy/Cargo.toml --no-default-features --features platform_osmesa
  - cargo test --verbose --manifest-path suzy_build_tools/Cargo.toml
